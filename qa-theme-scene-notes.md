# QA theme, cảnh nền và bố cục

- [x] Desktop 1280×720: màn hình đăng nhập hiển thị đầy đủ, vùng minh hoạ và biểu mẫu không chồng lấn.
- [x] Mobile 375×812: biểu mẫu co về một cột, nhãn và nút đăng nhập vẫn đọc được, không xuất hiện thanh cuộn ngang.
- [x] Regression xác nhận cảnh nền mặc định và mixer lấy dữ liệu từ `ProfileState`; các cảnh có hiệu ứng được áp dụng tại controller cấp ứng dụng sau đăng nhập.

