import fs from "node:fs";

const homePath = "client/src/pages/Home.tsx";
let home = fs.readFileSync(homePath, "utf8");
const importLine = 'import { emotionThemes, type EmotionId } from "@/lib/emotionThemes";';
const audioImport = importLine + '\nimport { DEFAULT_AMBIENT_MORNING_URL, DEFAULT_AMBIENT_RAIN_URL, DEFAULT_AMBIENT_STORM_URL, PROVIDED_THEME_AMBIENT_ASSETS } from "@/lib/defaultAmbient";';
home = home.replace(importLine, audioImport);
const fn = 'function AppearanceStudio({ profile, onProfile }: { profile: ProfileState; onProfile: (profile: ProfileState, message?: string) => void }) {';
const helper = '\nconst AUDIO_BACKED_SCENE_IDS = new Set(["morning", "rain", "storm", "tet", "space", "rainy_season", "stormy_season", "morning_chill", "coffee"]);\nconst AUDIO_BACKED_SCENE_AUDIO: Record<string, { label: string; url: string }> = { morning: { label: "Buổi sáng · Chim hót", url: DEFAULT_AMBIENT_MORNING_URL }, rain: { label: "Mưa dịu", url: DEFAULT_AMBIENT_RAIN_URL }, storm: { label: "Bão nhẹ", url: DEFAULT_AMBIENT_STORM_URL }, tet: { label: PROVIDED_THEME_AMBIENT_ASSETS.tet.name, url: PROVIDED_THEME_AMBIENT_ASSETS.tet.url }, space: { label: PROVIDED_THEME_AMBIENT_ASSETS.space.name, url: PROVIDED_THEME_AMBIENT_ASSETS.space.url }, rainy_season: { label: PROVIDED_THEME_AMBIENT_ASSETS.rainy_season.name, url: PROVIDED_THEME_AMBIENT_ASSETS.rainy_season.url }, stormy_season: { label: PROVIDED_THEME_AMBIENT_ASSETS.stormy_season.name, url: PROVIDED_THEME_AMBIENT_ASSETS.stormy_season.url }, morning_chill: { label: PROVIDED_THEME_AMBIENT_ASSETS.morning_chill.name, url: PROVIDED_THEME_AMBIENT_ASSETS.morning_chill.url }, coffee: { label: PROVIDED_THEME_AMBIENT_ASSETS.coffee.name, url: PROVIDED_THEME_AMBIENT_ASSETS.coffee.url } };\n';
if (!home.includes("const AUDIO_BACKED_SCENE_IDS")) home = home.replace(fn, helper + fn);
const oldReturn = fn + '\n  return';
const newReturn = fn + '\n  const [audioTheme, setAudioTheme] = useState<{ id: string; label: string; url: string } | null>(null);\n  const [audioVolume, setAudioVolume] = useState(42);\n  const [audioEnabled, setAudioEnabled] = useState(true);\n  const audioRef = useRef<HTMLAudioElement | null>(null);\n  const chooseAudioTheme = (scene: { id: string; label: string }) => { const audio = AUDIO_BACKED_SCENE_AUDIO[scene.id]; if (!audio) return; onProfile({ ...profile, defaultAmbientScene: scene.id as ProfileState["defaultAmbientScene"] }, `Đã áp dụng ${scene.label}.`); setAudioTheme({ id: scene.id, label: audio.label, url: audio.url }); };\n  const toggleThemeAudio = () => { const player = audioRef.current; if (!player || !audioEnabled) return; if (player.paused) { player.volume = audioVolume / 100; void player.play().catch(() => undefined); } else player.pause(); };\n  const updateThemeAudioVolume = (value: number) => { setAudioVolume(value); if (audioRef.current) audioRef.current.volume = value / 100; };\n  return';
if (!home.includes('aria-label="Điều khiển âm nền theme"')) home = home.replace(oldReturn, newReturn);
home = home.replaceAll('] as const).map((scene) => <button key={scene.id}', '] as const).filter((scene) => AUDIO_BACKED_SCENE_IDS.has(scene.id)).map((scene) => <button key={scene.id}');
home = home.replaceAll('onClick={() => onProfile({ ...profile, defaultAmbientScene: scene.id }, `Đã áp dụng cảnh ${scene.label}.`)}', 'onClick={() => chooseAudioTheme(scene)}');
const close = '  </div>;\n}\n';
const modal = '    {audioTheme ? <div role="dialog" aria-modal="true" aria-label="Điều khiển âm nền theme" className="fixed inset-0 z-[120] grid place-items-center bg-slate-950/45 p-4" onClick={() => setAudioTheme(null)}><div className="w-full max-w-md rounded-3xl border border-emerald-200 bg-white p-5 text-slate-900 shadow-2xl dark:border-emerald-400/25 dark:bg-slate-900 dark:text-slate-100" onClick={(event) => event.stopPropagation()}><div className="flex items-start justify-between gap-3"><div><p className="text-xs font-black uppercase tracking-[.14em] text-emerald-700 dark:text-emerald-300">Âm nền giao diện</p><h3 className="mt-1 text-xl font-black">{audioTheme.label}</h3><p className="mt-2 text-sm text-slate-600 dark:text-slate-300">Bật/tắt và điều chỉnh âm lượng.</p></div><button type="button" aria-label="Đóng điều khiển âm nền" onClick={() => setAudioTheme(null)} className="rounded-xl p-2"><X className="h-5 w-5" /></button></div><audio ref={audioRef} src={audioTheme.url} loop preload="none" onLoadedMetadata={(event) => { event.currentTarget.volume = audioVolume / 100; }} /><button type="button" onClick={toggleThemeAudio} className="mt-5 rounded-xl bg-emerald-600 px-4 py-2 text-sm font-black text-white">{audioEnabled ? "Bật / dừng nghe thử" : "Âm nền đang tắt"}</button><label className="mt-4 flex items-center gap-2 text-sm font-bold"><input type="checkbox" checked={audioEnabled} onChange={(event) => { setAudioEnabled(event.target.checked); if (!event.target.checked) audioRef.current?.pause(); }} />Cho phép âm nền</label><label className="mt-4 block text-sm font-bold">Âm lượng {audioVolume}%<input className="mt-2 w-full accent-emerald-600" type="range" min="0" max="100" value={audioVolume} onChange={(event) => updateThemeAudioVolume(Number(event.target.value))} aria-label="Âm lượng âm nền theme" /></label></div></div> : null}\n  </div>;\n}\n';
if (!home.includes('aria-label="Điều khiển âm nền theme"')) home = home.replace(close, modal);
fs.writeFileSync(homePath, home);

const expPath = "client/src/components/ExperienceStudio.tsx";
let exp = fs.readFileSync(expPath, "utf8");
exp = exp.replace('    {profile && onProfile ? <PersonalStudySpaceControls profile={profile} emotion={theme.id} onEmotion={onSelect} onProfile={onProfile} /> : null}\n', '');
const start = exp.indexOf('    <PersistentCollapsible storageKey="experience-ambient-audio"');
if (start >= 0) { const end = exp.indexOf('</PersistentCollapsible>', start); if (end < 0) throw new Error("Không tìm thấy điểm kết thúc panel audio"); exp = exp.slice(0, start) + exp.slice(end + '</PersistentCollapsible>'.length); }
if (!exp.includes('const AUDIO_BACKED_SCENE_IDS')) exp = exp.replace('const sceneOptions:', 'const AUDIO_BACKED_SCENE_IDS = new Set<AmbientScene>(["morning", "rain", "storm", "tet", "space", "rainy_season", "stormy_season", "morning_chill", "coffee"]);\nconst sceneOptions:');
exp = exp.replaceAll('sceneOptions.filter((scene) =>', 'sceneOptions.filter((scene) => AUDIO_BACKED_SCENE_IDS.has(scene.id) &&');
fs.writeFileSync(expPath, exp);
